pipeline {
 agent any 
    stages {
        stage('Build') {
            steps {
                sh 'ls'
            }
        }
        stage('Deliver') {
            steps {
                sh 'node --version'
            }
        }
    }
}
